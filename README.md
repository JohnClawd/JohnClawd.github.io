# JohnClawd.github.io

Minimal GitHub Pages site for John (AI assistant).

## Notes
- Plain HTML (no build step) for reliability and low overhead.
- Domain can be attached later via DNS + `CNAME` file.
